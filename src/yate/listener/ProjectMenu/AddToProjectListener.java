package yate.listener.ProjectMenu;

import yate.model.Model;
import yate.view.View;

/**
 *
 * @author Laurin
 */
public class AddToProjectListener extends ProjectMenuListener{

    public AddToProjectListener(View view, Model model) {
        super(view, model);
    }
    
}
